const GrayBadge = () => {
  return <span className="text-xs px-3 bg-gray-200 text-gray-800 rounded-full">Badge</span>
}

const RedBadge = () => {
  return <span className="text-xs px-3 bg-red-200 text-red-800 rounded-full">Badge</span>
}

const OrangeBadge = () => {
  return <span className="text-xs px-3 bg-orange-200 text-orange-800 rounded-full">Badge</span>
}

const YellowBadge = () => {
  return <span className="text-xs px-3 bg-yellow-200 text-yellow-800 rounded-full">Badge</span>
}

const LimeBadge = () => {
  return <span className="text-xs px-3 bg-green-200 text-green-800 rounded-full">Badge</span>
}

const TealBadge = () => {
  return <span className="text-xs px-3 bg-teal-200 text-teal-800 rounded-full">Badge</span>
}

const BlueBadge = () => {
  return <span className="text-xs px-3 bg-blue-200 text-blue-800 rounded-full">Badge</span>
}

const PurpleBadge = () => {
  return <span className="text-xs px-3 bg-purple-200 text-purple-800 rounded-full">Badge</span>
}

const IndigoBadge = () => {
  return <span className="text-xs px-3 bg-indigo-200 text-indigo-800 rounded-full">Badge</span>
}

const PinkBadge = () => {
  return <span className="text-xs px-3 bg-pink-200 text-pink-800 rounded-full">Badge</span>
}

const LargeGrayBadge = () => {
  return <span className="text-sm px-3 bg-gray-200 text-gray-800 rounded-full">Badge</span>
}

const LargeRedBadge = () => {
  return <span className="text-sm px-3 bg-red-200 text-red-800 rounded-full">Badge</span>
}

const LargeOrangeBadge = () => {
  return <span className="text-sm px-3 bg-orange-200 text-orange-800 rounded-full">Badge</span>
}

const LargeYellowBadge = () => {
  return <span className="text-sm px-3 bg-yellow-200 text-yellow-800 rounded-full">Badge</span>
}

const LargeGreenBadge = () => {
  return <span className="text-sm px-3 bg-green-200 text-green-800 rounded-full">Badge</span>
}

const LargeTealBadge = () => {
  return <span className="text-sm px-3 bg-teal-200 text-teal-800 rounded-full">Badge</span>
}

const LargeBlueBadge = () => {
  return <span className="text-sm px-3 bg-blue-200 text-blue-800 rounded-full">Badge</span>
}

const LargePurpleBadge = () => {
  return <span className="text-sm px-3 bg-purple-200 text-purple-800 rounded-full">Badge</span>
}

const LargeIndigoBadge = () => {
  return <span className="text-sm px-3 bg-indigo-200 text-indigo-800 rounded-full">Badge</span>
}

const LargePinkBadge = () => {
  return <span className="text-sm px-3 bg-pink-200 text-pink-800 rounded-full">Badge</span>
}

const DotBadge = () => {
  return <span className="bg-gray-500 rounded-full"></span>
}

export {
    DotBadge,
    IndigoBadge,
    LargeBlueBadge,
    LargeGrayBadge,
    LargeGreenBadge,
    LargeIndigoBadge,
    LargeOrangeBadge,
    LargePinkBadge,
    LargePurpleBadge,
    LargeRedBadge,
    LargeTealBadge,
    LargeYellowBadge,
    LimeBadge,
    OrangeBadge,
    PinkBadge,
    PurpleBadge,
    RedBadge,
    TealBadge,
    YellowBadge,
    BlueBadge,
    GrayBadge
}